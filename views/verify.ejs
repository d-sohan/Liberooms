<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= userHeader %> Verification</title>
</head>

<body>
    <h1>Liberooms</h1>
    <h2><%= userHeader %> Verification</h2>
    <form action="/<%= userType %>/verify" method="post">
        <div>
            <label for="code">Please enter the verification code sent to <%= userEmail %> (check spam folder too):</label>
        </div>
        <div>
            <input type="text" name="code" id="code" placeholder="Enter 16-character code" required>
        </div>
        <div>
            <input type="submit" name="submit" id="submit" value="<%= purpose %>">
        </div>
        <% if (dialog.msg !== "") { %>
        <% if (dialog.hasFailed === true) { %>
        <p style="color: red;">Verification failed: <%= dialog.msg %></p>
        <% } else {  %>
        <p style="color: green;"><%= dialog.msg %></p>
        <% } %>
        <% } %>
    </form>

</body>

</html>