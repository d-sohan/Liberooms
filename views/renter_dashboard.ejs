<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= userHeader %> <%= renterPtr.fname %>&apos;s Dashboard</title>
</head>

<body>
    <h1>Liberooms</h1>
    <h2><%= userHeader %> <%= renterPtr.fname %>&apos;s Dashboard</h2>
    <div>
        <a href="/renter/user/<%= renterPtr.username %>/houses/create">Register your houses</a>
    </div>
    <div>
        <a href="/renter/user/<%= renterPtr.username %>/houses/manage">Manage your houses</a>
    </div>
    <!-- <div>
        <a href="/renter/user/<%= renterPtr.username %>/ads/create">Create an Ad</a>
    </div>
    <div>
        <a href="/renter/user/<%= renterPtr.username %>/ads/manage">Manage your ads</a>
    </div> -->
    <div>
        <h3>Your profile</h3>
        <div>First Name: <%= renterPtr.fname %></div>
        <div>Last Name: <%= renterPtr.lname %></div>
        <div>Email: <%= renterPtr.email %></div>
        <div>Username: <%= renterPtr.username %></div>
    </div>
    <% if (dialog.msg !== "") { %>
    <% if (dialog.hasFailed === true) { %>
    <p style="color: red;">Failed: <%= dialog.msg %></p>
    <% } else {  %>
    <p style="color: green;"><%= dialog.msg %></p>
    <% } %>
    <% } %>



    <form action="/renter/logout/<%= renterPtr.username %>" method="post">
        <input type="submit" value="Log out">
    </form>
</body>

</html>